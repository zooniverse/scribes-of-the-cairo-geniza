.crib-sheet
  &__header
    @extend .secondary-label
    font-size: 0.75rem

    button
      background-color: inherit
      border-radius: 1rem
      border: none
      color: $plum
      margin: 0.5rem
      padding: 0.25rem 0.75rem
      text-transform: uppercase

      &:hover, &:focus, &.active-button
        background-color: $sand

  &__reference, &__personal
    margin: 1rem

  &__card
    @extends .body-font
    background-color: inherit
    display: flex
    flex-direction: column
    font-size: 0.75rem
    font-weight: 600
    padding: 0

  &__item
    background-color: transparent
    margin: 0.5rem
    position: relative

    &:hover, &:focus
      .crib-sheet__delete
        visibility: visible

  &__delete
    background-color: $plum
    border-radius: 50%
    color: white
    font-size: 0.5rem
    padding: 0.2rem 0.3rem
    position: absolute
    right: -5px
    top: -5px
    visibility: hidden
    z-index: 1

  &__personal-content
    display: flex
    flex-wrap: wrap

    button
      border: none

    img
      border: 2px solid $plum
      width: 6rem
      height: 6rem

      &:hover, &:focus
        opacity: 0.5

  &__personal-instructions
    border-bottom: 3px solid $plum
    margin-bottom: 1rem
    padding-bottom: 1rem

    span
      @extend .body-font
      display: block
      margin-bottom: 0.25rem

      &:first-child
        font-weight: 600

  &__personal-card
    @extend .tertiary-label
    margin: 0.5rem
    width: 6rem

    div:first-child
      height: 6rem
      border: 2px solid $plum
      display: flex
      margin-bottom: 0.5rem

    button
      background-color: inherit
      width: 100%

  &__add-instructions
    font-weight: 400

.save-snippet
  align-items: center
  display: flex
  margin-top: 5rem
  flex-direction: column

  input
    font-size: 0.8em
    padding: 0.5em
    width: 15em

  &__image
    text-align: center
    margin: 0.5em
    height: 9em
    width: 9em

    img
      object-fit: contain
      overflow: hidden
      height: 100%
      width: 100%

  &__buttons
    font-size: 0.7em

    button
      margin: 1.5em
      width: 7em
      padding: 0.5em 1.5em
